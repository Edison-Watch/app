name: AI Writing Check (desktop)
on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'packages/desktop/**'
jobs:
  ai-writing-check:
    name: AI Writing Check (desktop)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/desktop
    steps:
      - uses: actions/checkout@v7
      - uses: oven-sh/setup-bun@v2
      - run: bun scripts/check-ai-writing.ts
